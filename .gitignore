# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# secrets — never commit
.env
.env.*
*recovery-codes*
*recovery_codes*
*backup-codes*

# loose documents dropped in the repo root (site PDFs live in public/)
/*.pdf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# scratch / generated artifacts
.playwright-mcp/
*.bak
*.bak/
.~lock.*#

# local-only folders (personal docs + design experiments)
business-docs/
experiments/

# local agent/session config — never deploy
.claude/

# raw uncompressed video originals (compressed versions are tracked)
public/videos/_originals/

# favicon source artwork (the resized icons are tracked)
public/favicon-full.png
public/favicon-source.jpg
